import React from 'react';
import Template from '../template/Template';

const Acciones =()=>{
    return (
        <Template>
            <button type="button" className="btn btn-info">aceptar</button>
        </Template>
    );
}
export default Acciones;